name: "Bruce Constraint‑Aware Decision Module (BCADM)"
description: "Runs the BCADM multi‑constraint decision loop for agent and robotics workflows."
author: "Bruce"

inputs:
  steps:
    description: "Number of simulation steps to run"
    required: false
    default: "10"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "cpu"
  color: "blue"
